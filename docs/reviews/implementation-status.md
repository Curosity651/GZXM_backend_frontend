# 阶段1与阶段2-A执行结果

日期：2026-09-15。

## 阶段1

| 项目 | 结果 |
|---|---|
| 前端基线 | 来源分支 `feat/requirements-aligned-prototype`，基线提交 `cbbd56f` 已推送远端并复制到本仓库 `frontend/` |
| API契约 | OpenAPI 3.1 共67个操作，规范校验通过；Markdown可读版同步保留 |
| 数据库基线 | 27张表已转换为首版Flyway迁移；隔离MySQL 8首次迁移成功，再次启动显示无重复迁移 |
| 公共Stub | 已提供 `CurrentUser`、`TopicAccessService`、统一异常、分页、审计和文件存储接口 |
| 前端联调基础 | 已提供API模式、统一HTTP Client、JWT附加、401单次刷新与会话失效事件 |
| 协作基线 | 已提供A/B/C任务单、任务模板、评审清单、模块所有权及迁移规则 |

## 阶段2中的A任务

| 范围 | 已实现内容 |
|---|---|
| 认证 | 登录、当前用户、JWT访问令牌、Redis刷新令牌单次轮换、注销、账号版本失效 |
| 系统管理 | 用户查询/创建/编辑/启停/重置密码，固定角色、权限配置、单位查询 |
| 权限底座 | RBAC动作权限、课题成员/牵头/内外部/所属单位校验；外部单位自筹权限不可开启 |
| 文件底座 | Mock存储适配器、上传票据、完成上传、预览/下载签名地址；可替换MinIO/S3 |
| 工程与集成 | Java 21 Maven Wrapper、MySQL/Redis编排、Nginx、环境脚本、Flyway与Swagger UI |

## 验证记录

- 后端：13项认证、角色锁定、课题数据范围与文件越权测试通过。
- 前端：94项测试通过，代码检查通过，生产构建通过。
- 数据库：27张表创建成功；种子数据为5个角色、29个权限、1个管理员。
- 契约：67个operationId，OpenAPI规范校验通过。
- 当前机器的Docker Desktop未启动，因此Redis未做真实进程联调；刷新令牌行为已由单元测试覆盖，编排文件已通过静态配置检查（提供`.env`时）。
