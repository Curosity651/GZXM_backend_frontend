# 2026-09-14 阶段成果归档

本目录单独保存 2026-09-14 形成的项目成果索引和后续方案。其目的不是复制整个代码仓库，而是留下一个清晰的阶段快照：今天完成了什么、当前能运行到什么程度、哪些内容还只是方案、下一阶段应按什么顺序推进。

## 目录内容

```text
2026-09-14/
├── README.md
├── files/
│   └── 成果文件清单.md
└── plans/
    ├── 项目总体结构与功能方案.md
    └── 下一阶段开发计划.md
```

## 今日成果摘要

1. 建立 Java + Spring Boot 后端基础工程，形成认证、权限、项目主数据的初始代码。
2. 建立四人协作分工、模块边界、Codex 使用共识、分支和审查规则。
3. 将本地数据库 YAML 配置从 PostgreSQL 调整为 MySQL 8.4。
4. 按要求关闭 Hibernate 自动建表、Flyway 迁移、SQL 初始化和本地初始化数据。
5. 梳理当前前端 Mock 原型、后端已实现能力与 6 个初始业务 API。
6. 形成完整目标项目结构、功能模块和分阶段开发方案，但不在今天继续创建业务数据表或扩展业务模块。

## 当前状态结论

- 前端：已有较完整的 Mock 业务原型，数据主要保存在浏览器 `localStorage`，尚未接入真实后端。
- 后端：已有 `auth` 与 `project` 基础代码，其他业务模块尚未实现。
- 数据库：MySQL 容器和数据源 YAML 已配置，但 MySQL JDBC/Flyway 依赖尚未切换，也没有 MySQL 数据表。
- API：已有登录、当前用户、项目、单位和课题的初步接口；全系统 API 契约尚未完成。
- 部署：尚未进入阿里云部署阶段。
- 验证环境：当前机器缺少可用的 Node.js/npm、JDK 运行时和 Docker，因此本阶段没有完成真实启动验证。

## 阅读顺序

1. 先读 [成果文件清单](files/成果文件清单.md)，确认今天新增或调整了哪些内容。
2. 再读 [项目总体结构与功能方案](plans/项目总体结构与功能方案.md)，理解完整目标和模块边界。
3. 最后读 [下一阶段开发计划](plans/下一阶段开发计划.md)，按顺序创建后续任务。

## 保存规则

- 本目录保存方案和文件索引，实际代码仍以仓库中的原始路径为唯一版本。
- 不复制 `server/`、`src/`、`node_modules/` 或构建产物，避免出现两套代码和版本漂移。
- 后续如果方案发生实质变化，应创建新的日期目录，不覆盖本次阶段结论。
- 本目录中的“计划”不等于已经实现，必须以成果清单和实际代码为准。
